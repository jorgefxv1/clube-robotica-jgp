CHECKLIST — Colocar o site no ar (Clube de Robótica JGP)
==========================================================

Legenda: [ ] em aberto   [x] concluído   [~] aceito   [N/A] não aplicável

----------------------------------------------------------
1. HOSPEDAGEM
----------------------------------------------------------
[x] 1.1 Escolher provedor gratuito: Cloudflare Pages (Workers)
        -> URL atual: https://clube-robotica-jgp.jorge-fxvx.workers.dev
[x] 1.2 Deploy automático via GitHub (main branch)
[x] 1.3 Arquivo _redirects criado — /admin → admin.html (commit 1ca3eb9)

----------------------------------------------------------
2. VERSIONAMENTO (GIT)
----------------------------------------------------------
[x] 2.1 Rodar `git init` na pasta do projeto
        -> repo criado em ./site/ (isolado dos arquivos pessoais)
        -> branch padrão: main
[x] 2.2 Criar .gitignore (excluir arquivos pessoais, .env, etc.)
        -> site/.gitignore (DS_Store, .env, logs, IDEs, node_modules)
[x] 2.3 Criar repositório no GitHub
        -> https://github.com/jorgefxv1/clube-robotica-jgp (público)
[x] 2.4 Primeiro commit + push
        -> commit c7723b3 + push para origin/main OK
        -> credencial PAT salva no Keychain (osxkeychain helper)

----------------------------------------------------------
3. PONTO DE ENTRADA DO SITE
----------------------------------------------------------
[x] 3.1 Decidir: renomear robotica-site.html -> index.html
        -> renomeado para site/index.html
[x] 3.2 Conferir todos os links internos após a mudança
        -> admin.html: href "robotica-site.html" -> "index.html" (linha 952)

----------------------------------------------------------
4. SUPABASE — SEGURANÇA (CRÍTICO)
----------------------------------------------------------
[x] 4.1 Listar tabelas existentes (list_tables)
        -> teams (5), members (19), history_events (0), blog_posts (2)
[x] 4.2 Rodar get_advisors para ver alertas de segurança
        -> 3 WARNs (nenhum ERROR): buckets blog-covers e history-photos
           permitem listagem; leaked password protection desativado
[x] 4.3 Confirmar que RLS (Row Level Security) está ligado em
        TODAS as tabelas
        -> OK: RLS ativo nas 4 tabelas do schema public
[x] 4.4 Revisar políticas RLS: leitura/escrita só para quem deve
        ACHADO: leitura pública OK, mas TODOS os autenticados têm
        ALL (INSERT/UPDATE/DELETE) em todas as tabelas.
        Risco: se signup público estiver aberto, qualquer um cria
        conta e edita tudo.
[x] 4.5 Verificar se existe tabela/coluna de "role" (admin)
        ACHADO: NÃO existe. Hoje só há 1 usuário (Jorge).

[x] 4.5.b Criar estrutura de role (migration aplicada 2026-05-07):
          [x] Tabela public.profiles(user_id, role, created_at)
          [x] Trigger handle_new_user (cria profile no signup)
          [x] Função public.is_admin()
          [x] Policies admin_all_* nas 4 tabelas
          [x] Policies em profiles (user lê o próprio; só admin altera)
          [x] Jorge promovido a admin
[x] 4.6 Restringir SELECT dos buckets blog-covers e history-photos
        -> SELECT amplo removido (URL direta segue funcionando)
        -> upload/delete/update apertados para exigir is_admin()
        -> WARNs dos buckets sumiram do advisor
[N/A] 4.7 "Leaked Password Protection" exige plano Pro do Supabase.
          Projeto está no Free — funcionalidade indisponível.
          Mitigação: orientar admins a usar senhas fortes/únicas.
[~] 4.8 (aceito) is_admin() é SECURITY DEFINER chamável por
        authenticated — INTENCIONAL: o admin.html usa rpc('is_admin')
        para validar role. Linter alerta mas não é vulnerabilidade.

----------------------------------------------------------
5. SUPABASE — CONFIGURAÇÃO DE URLS
----------------------------------------------------------
[~] 5.1 Authentication > URL Configuration: adicionar Site URL pública
        -> FAZER MANUALMENTE: supabase.com > projeto > Auth > URL Configuration
        -> Site URL: https://clube-robotica-jgp.jorge-fxvx.workers.dev
[~] 5.2 Adicionar Redirect URLs:
        -> https://clube-robotica-jgp.jorge-fxvx.workers.dev/**
[ ] 5.3 API > CORS: liberar o domínio público (se necessário)

----------------------------------------------------------
6. PROTEÇÃO DO ADMIN.HTML
----------------------------------------------------------
[ ] 6.1 Garantir que admin.html exige login antes de carregar dados
[x] 6.2 No admin.html, chamar rpc('is_admin') após login e bloquear
        UI se retornar false (RLS já protege os dados; isso é UX)
        -> implementado em onAuthStateChange (admin.html:1197)
[ ] 6.3 Testar acesso anônimo a /admin.html (deve bloquear dados)
[ ] 6.4 Testar com usuário "member" recém-criado (não pode editar nada)

----------------------------------------------------------
7. DOMÍNIO
----------------------------------------------------------
[ ] 7.1 Usar subdomínio gratuito do provedor (*.pages.dev etc.)
[ ] 7.2 (Opcional, futuro) Comprar domínio próprio (.com.br ~R$40/ano)
[ ] 7.3 (Opcional) Configurar DNS no provedor

----------------------------------------------------------
8. TESTES EM PRODUÇÃO
----------------------------------------------------------
[ ] 8.1 Abrir o site no celular, fora da rede da escola
[ ] 8.2 Testar cadastro e login
[ ] 8.3 Testar fluxos do admin
[ ] 8.4 Verificar Console do navegador (erros CORS, 401, etc.)
[ ] 8.5 Testar tema claro/escuro e responsividade

----------------------------------------------------------
9. PENDÊNCIAS ABERTAS (retomar depois)
----------------------------------------------------------
[x] 9.1 Investigar relato do Jorge: "index.html está fora, não
        carrega algumas informações" (2026-05-08)
        CAUSA RAIZ ENCONTRADA E CORRIGIDA (2026-05-12) — ver também 9.4:
        -> blog_posts retornava 401; history_events retornava 200
        -> Postgres log: "permission denied for function is_admin"
        -> A policy admin_all_blog_posts (roles=public) chama is_admin()
           mas o role 'anon' não tinha EXECUTE na função
        -> history_events funcionava por short-circuit: sua policy
           pública é `true` (trivialmente verdadeira), então o banco
           nunca chegava a avaliar is_admin()
        -> blog_posts falha porque sua policy pública é `published=true`
           (não-trivial), forçando avaliação de is_admin() → erro
        CORREÇÃO APLICADA: GRANT EXECUTE ON FUNCTION public.is_admin() TO anon
        -> migration: grant_is_admin_to_anon
        -> history_events (4 registros) e blog_posts (2 publicados)
           devem carregar normalmente agora

[ ] 9.2 Compartilhamento do projeto com colega (Claude Code Pro):
        já preparado o caminho (CLAUDE.md + checklist no repo,
        commit cee9c69), mas falta:
        -> Adicionar colega como collaborator no GitHub
           (Settings > Access > Add people)
        -> Decidir workflow: commits diretos na main vs branches+PR
        -> Combinar regra: sempre `git pull` antes de editar
        -> Avaliar se colega também precisa virar admin no Supabase

[x] 9.5 Upload de capa do blog retornava 400 (2026-05-12):
        CAUSA: storage.objects não tinha policy SELECT para blog-covers
        e history-photos. O upsert precisa de SELECT para checar se
        o arquivo existe antes de inserir/atualizar.
        CORREÇÃO: policies "public read blog covers" e
        "public read history photos" criadas (migration:
        add_select_policies_storage_buckets).
        BONUS: storage policies reescritas com query direta em vez de
        is_admin() para evitar problemas de schema context.

[ ] 9.3 Próximos passos do deploy (ordem sugerida):
        a) Seção 1 — Hospedagem (Cloudflare Pages recomendado;
           conecta direto ao GitHub para deploy automático)
        b) Seção 5 — URLs no Supabase (Site URL, Redirect URLs,
           CORS) apontando para o domínio público
        c) Seção 6 — Testes do admin (6.1, 6.3, 6.4)
        d) Seção 7 — Domínio (subdomínio *.pages.dev grátis primeiro)
        e) Seção 8 — Testes em produção (mobile, fora da escola)

[x] 9.4 Corrigir acesso /admin (2026-05-12):
        CAUSAS:
        a) Cloudflare Pages não mapeava /admin → admin.html
           -> CORRIGIDO: arquivo _redirects criado (commit 1ca3eb9)
        b) admin.html chamava rpc('get_my_role') mas função não existia
           -> CORRIGIDO: função get_my_role() criada no Supabase
              (migration: create_get_my_role_function)
        c) Supabase Auth: Site URL e Redirect URLs não configuradas
           -> PENDENTE: fazer manualmente (ver seção 5.1 e 5.2)

==========================================================
Última atualização: 2026-05-12 (correções /admin; retomar pela seção 5.1/5.2)
